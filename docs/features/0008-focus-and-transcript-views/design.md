# Feature 0008：当前句与全文双视图

状态：设计中

## 1. 结论

课程正文改为两个互斥视图：

- **当前句**：进入课程时默认显示，只展示正在播放的句子；
- **全文**：展示完整字幕，高亮并跟随当前播放句。

页面提供“当前句 / 全文”按钮，也支持在正文区域左滑进入全文、右滑返回当前句。切换视图只改变呈现，
不暂停、不播放、不 seek，也不改变逐句循环或保存进度。

## 2. 界面与行为

课程页保留三块稳定区域：

```text
[ 返回课程 ]
课程标题                         [删除本地课程]

[ 当前句 ] [ 全文 ]

┌──────────────────────────────┐
│ 当前选择的正文视图             │
└──────────────────────────────┘

[ 固定播放器底栏 ]
```

课程标题下方增加正文视图切换：

```text
[ 当前句 ] [ 全文 ]
```

使用两个普通按钮和 `aria-pressed` 表示选中状态，触控高度不小于 44px。手势只是快捷方式，不能作为唯一
入口。

### 2.1 当前句

- 只渲染现有 `.focus-sentence`，不同时渲染全文列表。
- 继续显示句子序号、说话人、英文和可用翻译。
- 内容随 `currentSegment` 更新。
- 短内容在标题和播放器底栏之间尽量居中；长句和横屏允许页面纵向滚动，不裁切内容。

### 2.2 全文

- 只渲染现有 `.transcript`，不重复显示大型当前句卡片。
- 当前句继续使用 `.segment.active`，点击字幕继续调用 `seekToSegment`。
- 进入全文时将当前句滚动到播放器底栏以上的可见区域。
- 默认跟随播放：当前句离开安全可视区域时，将其滚动回接近视口中部。
- 用户通过触摸或滚轮主动纵向浏览后暂停跟随，并显示“回到当前句”；点击后定位当前句并恢复跟随。
- 每次重新进入全文时恢复跟随。

阅读模式继续复用 `showTranslation` / `show-translation`，在两个视图中保持一致。

## 3. 手势

- 当前句左滑进入全文；全文右滑返回当前句。
- 只监听标题下方的正文容器，不监听页面、标题或固定播放器底栏。
- 水平距离至少约 60px，且 `absX > absY * 1.25` 时才触发。
- 从屏幕左侧 24px 内开始的右滑留给浏览器或系统返回手势。
- 正文使用 `touch-action: pan-y`，保留纵向滚动。
- 只处理主指针；`pointerup` 或 `pointercancel` 后必须清理手势状态。
- 成功识别横滑后，抑制同一次触摸产生的字幕点击；普通轻触字幕不受影响。
- 首版在手势结束后切换，不实现跟手轮播动画，也不引入手势库。

## 4. 状态

在 `App.tsx` 中增加局部状态：

```ts
type PlayerView = "focus" | "transcript";

const [playerView, setPlayerView] = useState<PlayerView>("focus");
const [followingTranscript, setFollowingTranscript] = useState(true);
```

- 打开、切换或退出课程时重置为 `focus`。
- 进入全文时将 `followingTranscript` 重置为 `true`。
- 用户通过触摸或滚轮主动浏览全文后将其设为 `false`，不能用程序滚动也会触发的普通 `scroll` 事件判断。
- 两个状态都不持久化。
- 切换视图不得修改 `currentTime`、`playing`、`loopSegment`、`repeatIteration` 或循环切换锁。

手势起点、活动指针和当前字幕元素使用少量 `ref` 保存，不在移动过程中反复更新 React state。

## 5. 全文定位与性能

当前高亮字幕复用单个 ref：

```ts
const activeSegmentRef = useRef<HTMLButtonElement>(null);
```

进入全文或跟随播放时，仅在当前句离开顶部内容区与固定底栏之间的安全区域后滚动。使用
`scrollIntoView({ block: "center" })` 时必须配合底栏留白或 `scroll-margin`，确保目标不被遮挡；
`prefers-reduced-motion: reduce` 下取消平滑滚动。

全文列表不能因 `currentTime` 的连续更新而每次重新执行完整 `map`。可直接在 `App.tsx` 使用 `useMemo`，只在
`course`、`currentSegment`、`showTranslation` 等真正影响列表的值变化时重新生成。不拆新组件，不引入虚拟
列表；如长课程仍有性能问题，再单独设计虚拟化。

## 6. 与播放器的关系

- Feature 0007 的底栏结构、菜单、安全区和时间轴保持不变。
- 点击正文切换按钮属于菜单外部点击，按现有规则关闭播放器菜单。
- Feature 0006 的循环目标、遍数、统一完成逻辑和切换锁保持不变。
- 全文点击字幕继续沿用现有 seek 和循环重置语义。
- 前台 RAF、后台 `onTimeUpdate`、锁屏、下载、离线和进度保存不变。

## 7. 实现范围

主要修改：

```text
src/App.tsx
src/app.css
```

实现内容：

- 增加两个正文视图及可见切换按钮；
- 条件渲染现有当前句和全文区域；
- 增加全文跟随、暂停跟随和“回到当前句”；
- 增加正文区域的最小手势处理；
- 增加窄屏、长句、安全区和 reduced-motion 样式。

保持代码直接，不拆组件，不抽通用 hook/service，不增加 Context、路由、状态管理、UI 或手势依赖，不修改
数据库、课程数据、Service Worker 和已经工作的播放函数。

## 8. 验证

自动检查：

```bash
pnpm build
git diff --check
```

浏览器与真机验证：

1. 进入课程默认只显示当前句，切换全文后不重复显示当前句卡片。
2. 当前句随播放更新；全文高亮并在未手动浏览时保持可见。
3. 用户纵向浏览全文后停止跟随，“回到当前句”能够恢复定位与跟随。
4. 点击字幕仍正确 seek，并保持现有循环重置语义。
5. 左滑进入全文、右滑返回；纵向滚动、轻触字幕和系统返回手势不被误判。
6. 底栏、时间轴和菜单操作不会触发视图切换。
7. 切换视图不改变播放位置、播放状态、倍速、循环目标或当前遍数。
8. 阅读模式在两个视图中同步生效。
9. 320px、375px、横屏和桌面宽度没有溢出、裁切或底栏遮挡。
10. 键盘可以操作两个视图按钮，并能识别 `aria-pressed` 状态。
11. 前后台、锁屏、离线播放、下载和进度恢复无回归。
12. 在真实 iPhone Safari 和主屏幕 Web App 中验证横滑、返回手势、纵向滚动和字幕点击。

## 9. 验收标准

- 默认只显示当前句，并可通过按钮或手势进入全文。
- 当前句与全文不会同时展示。
- 全文默认跟随当前句，用户浏览后暂停，并可恢复跟随。
- 手势不干扰系统返回、纵向滚动、字幕点击或播放器控制。
- 视图切换不影响播放、seek、循环、下载、离线和进度。
- 长句和窄屏不裁切，全文最后一句不被底栏遮挡。
- 完整字幕不会随每次 `currentTime` 更新重新生成。
- 不增加依赖、路由、数据库字段或新的架构分层。

## 10. 不实现

- 跟手轮播或物理惯性动画；
- 逐词高亮、波形、封面或播放队列；
- 上一句和下一句卡片预览；
- 横滑切换课程；
- 持久化正文视图；
- 虚拟列表或课程数据格式变更。
